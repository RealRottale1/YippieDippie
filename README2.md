-- 6 
We used any instead of max which resulted in the correct output but was merly coincidence and was instead a logical error. Any means if just 1 exist while MAX checks if they all do.

-- 10
We merged unrelated data into a single table. We neglected to use boohbah_stand_link and thus the data was unrelated similar to question 2.